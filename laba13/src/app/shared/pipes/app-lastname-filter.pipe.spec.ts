import { AppLastnameFilterPipe } from './app-lastname-filter.pipe';

describe('AppLastnameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AppLastnameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
