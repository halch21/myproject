import { AppFirstnameFilterPipe } from './app-firstname-filter.pipe';

describe('AppFirstnameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AppFirstnameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
